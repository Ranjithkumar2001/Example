 Collections.sort(host_throughput, Collections.reverseOrder()); 
        long maxThroughput = 0;
        int n = host_throughput.size();
        
        for (int i = 1; i + 1 < n; i += 3) {
            maxThroughput += host_throughput.get(i);
        }
        
        return maxThroughput;
